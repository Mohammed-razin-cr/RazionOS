[ollama]
; The adapter is local-only unless allow_remote is explicitly enabled.
host=127.0.0.1
port=11434

; Leave model empty to select the first model returned by /api/tags.
model=
timeout_ms=4000
max_tokens=192
allow_remote=0

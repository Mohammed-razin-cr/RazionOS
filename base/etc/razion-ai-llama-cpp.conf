[llama_cpp]
; VirtualBox NAT exposes the Windows host at 10.0.2.2.
host=10.0.2.2
port=8080

; Leave empty to use the model advertised by llama-server /v1/models.
model=
timeout_ms=30000
max_tokens=192
allow_remote=1
